# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env na raiz do projeto e ajuste os valores

# URL base da API (backend NestJS)
# Em produção na Azure VM, use o domínio/IP da sua VM
# IMPORTANTE: O código usa Uri.https(), então sempre será HTTPS
# Use apenas o domínio/IP sem http:// ou https://
# Exemplo: BASE_URL=api.seudominio.com ou BASE_URL=seudominio.com
BASE_URL=localhost:3000
